/** @type {import('next').NextConfig} */ 
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com', 
            'cms.travelworld.nl'
        ],
    },
};

export default nextConfig;